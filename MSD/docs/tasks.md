# Tasks

After extraction, MSD task directories are expected under:

```text
data/raw/extracted/
```

Each task should contain `dataset.json`, `imagesTr/`, and `labelsTr/`. Some
tasks may also include `imagesTs/`.
