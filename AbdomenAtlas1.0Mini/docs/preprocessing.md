# Preprocessing

No canonical local preprocessing pipeline has been added yet.

Recommended next steps:

- decide the target training framework, such as MONAI or nnU-Net;
- document spacing, orientation, intensity clipping, and resampling choices;
- write processed outputs under `data/processed/`;
- keep raw `BDMAP_*` directories unchanged.
